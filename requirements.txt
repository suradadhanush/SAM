# SAM — Python Dependencies
# Install: pip install -r requirements.txt
# Then: playwright install chromium

# ─── Core ──────────────────────────────────────────────────────────────────
requests>=2.31.0
pyyaml>=6.0.1

# ─── The Ears ──────────────────────────────────────────────────────────────
faster-whisper>=1.0.0
pyaudio>=0.2.14
openwakeword>=0.6.0
soundfile>=0.12.1
sounddevice>=0.4.6
numpy>=1.24.0

# ─── The Mouth ─────────────────────────────────────────────────────────────
kokoro>=0.9.4                  # Primary TTS (Apache 2.0)
# Windows only — installed automatically on Windows:
# pyttsx3                      # pip install pyttsx3

# ─── The Hands ─────────────────────────────────────────────────────────────
pyautogui>=0.9.54
playwright>=1.44.0
Pillow>=10.0.0                 # PIL.ImageGrab for Windows screenshots
# Windows only:
# pywinauto                    # pip install pywinauto

# ─── Memory ────────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ─── Ecosystem (Telegram bridge — Phase 2) ─────────────────────────────────
python-telegram-bot>=21.0      # Bot API client (long-polling, no server needed)
qrcode[pil]>=7.4.2              # Pairing QR code generation

# ─── Licensing (Phase 3) ────────────────────────────────────────────────────
cryptography>=42.0.0            # Ed25519 signing/verification for offline license checks

# ─── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
